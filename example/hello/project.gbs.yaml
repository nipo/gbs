name: simple_project
topcell: top
output_format: filelist

# Use the simulation profile defined in ../.gbs.yaml
# This provides: filter_vars, backends, and repository configuration
profile: simulation

root_library:
  name: root
  partitions:
    - name: top
      deps:
        - nsl_data.text
        - nsl_simulation.assertions
      sources:
        - language: vhdl
          files:
            - top.vhd

