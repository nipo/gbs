name: blink

root:
  name: top
  deps:
    - nsl_hwdep.clock
  sources:
    - file_type: vhdl
      files:
        - blink.vhd
    - file_type: ice40-pcf
      files:
        - blink.pcf

output:
  - name: pnr
    topcell: top
    target:
      part: up5k
      package: sg48
    backend_config:
      gbs.builtin.ghdl:
        vhdl_standard: "1993"
      gbs.builtin.yosys:
        tool: "yosys"
        steps:
          - flatten
          - tribuf -logic
          - deminout
      gbs.builtin.nextpnr:
        tool: "nextpnr-ice40"
      gbs.builtin.icepack:
        tool: "icepack"
    outputs:
      - type: bitstream
        path: blink.bin
      - type: synthesis-report
        path: synthesis-report.html
      - type: pnr-report
        path: pnr-report.html
